import React from 'react';

const Header = () => {
    return (
        <header>
            <a href="">
                <h1>Worldcup Draw</h1>
            </a>
        </header>
    );
};

export default Header;
