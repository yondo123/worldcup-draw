import React from 'react';
import {Heading} from '@chakra-ui/react';

const Header = () => {
    return (
        <header>
            <a href="">
                <Heading color={'draw.font'} size={'lg'}>
                    Worldcup Draw
                </Heading>
            </a>
        </header>
    );
};

export default Header;
